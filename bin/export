#!/bin/bash

if [[ ! -f .env ]]
then
  echo "The .env file doesn't exists!"
  echo "Please, run 'bin/setup' script first!"
  exit 1
fi

if [ -n "$1" ] && [ -n "$2" ]; then
  bundle exec foreman export upstart $1 -a sg-alpaca -u $2
else
  echo "Usage: bin/export <path> <user>"
  exit 1
fi
